.playwright-mcp/
mocks/*/node_modules/

# Clojure
.cpcache/
.nrepl-port
target/

# clj-kondo / LSP caches
.clj-kondo/.cache/
.lsp/.cache/

# Shadow CLJS
.shadow-cljs/
node_modules/
resources/public/js/
package-lock.json

# Host-specific symlink (points to config/<hostname>.edn)
resources/smithr.edn

# Secrets and generated certs — NEVER commit keys, certs, or env files
.env
tls/*.pem
layers/scripts/ios/ssh/macos-ssh-key
