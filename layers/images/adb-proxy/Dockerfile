# Minimal ADB proxy image — socat + android-tools for ADB healthchecks.
# Used by physical-phone.yml as a TCP proxy with real ADB verification.
FROM alpine:latest
RUN apk add --no-cache socat android-tools
